name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install main dependencies
      run: npm install
      working-directory: .

    - name: Install special test dependencies
      run: npm install
      working-directory: ./tests/functions

    - name: Run main tests
      run: npm test
      working-directory: .

    - name: Run special tests
      run: npm run test
      working-directory: ./tests/functions